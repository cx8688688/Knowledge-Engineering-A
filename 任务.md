任务：
A.知识图谱引擎(A1、A2组)
核心功能:·知识工程知识点本体建模(覆盖知识工程全课程内容)
图谱可视化(力导向图、层级图、时间线)
·基础查询(关键词搜索、关系遍历、路径发现)
推理功能(学习路径推荐、前置知识检测、知识点关联推荐)
知识图谱维护(提供人工进行节点、关系的增删改查
AI增强:·LLM辅助生成知识点描述
·基于图嵌入的相似知识点推荐
自然语言查询转Cypher查询

技术选型简要介绍
1. 前端：React / Vue
React​ (Facebook开源)：组件化开发典范，生态庞大。配合 Ant Design​ 组件库可快速搭建管理后台；使用 G6​ 或 ECharts​ 等专业图表/图可视化库，能高效实现知识图谱的力导向图、层级树等交互可视化。
Vue​ (尤雨溪团队)：渐进式框架，上手曲线平缓，官方生态（Vue Router, Vuex/Pinia）完整。配合 Element Plus​ 或 Ant Design Vue​ 同样可快速开发。两者皆适合中型项目，选择可依据小组熟悉度。
2. 后端：FastAPI (Python)
现代异步框架：基于 Python 3.6+ 的异步支持，性能优异，尤其适合处理大量 I/O 操作（如数据库查询、AI 服务调用）。
开发效率高：自动生成交互式 API 文档（Swagger UI），类型提示（Pydantic）使接口定义清晰、错误减少。
与AI栈天然融合：Python 是机器学习/深度学习的主流语言，方便直接集成 LangChain、图嵌入模型等，无需跨语言调用。
3. 图数据库：Neo4j
原生图存储：以“节点-关系-属性”方式存储，与知识图谱的“实体-关系-属性”模型完全契合，查询效率高。
Cypher 查询语言：声明式、直观易学（类似 SQL for Graph），便于表达复杂的图遍历、路径查询，极大降低后端开发复杂度。
活跃社区与工具：拥有 Neo4j Browser（可视化管理）、APOC 过程库等丰富工具，适合学术与工业场景。
4. AI 服务：LangChain + 国内大模型 API
LangChain：一个用于开发由大语言模型驱动的应用的框架。它提供了链（Chains）、代理（Agents）、记忆（Memory）​ 等高级抽象，可快速搭建“自然语言转 Cypher 查询”、“基于上下文的问答”、“图描述生成”等场景，避免从零编写Prompt管理、会话记录等底层逻辑。
国内大模型 API：如百度文心、阿里通义、智谱GLM、月之暗面等提供的 API，符合国内网络环境，稳定性好，且通常成本可控。通过 LangChain 可轻松切换不同供应商的模型接口。
5. 部署与协作：Docker + Git + GitHub Projects
Docker：容器化技术。可将 Neo4j、后端、前端等每个服务封装为独立镜像，实现环境一致、一键部署，避免“在我机器上能跑”的问题。配合 docker-compose 可轻松编排多个服务。
Git：版本控制系统。用于代码版本管理、协作开发。推荐工作流：主分支（main/prod）用于稳定版本，开发分支（dev）用于集成，功能分支（feature/xxx）用于开发新特性。
GitHub Projects：内置于 GitHub 的项目管理工具。可创建看板（To Do, In Progress, Done）、关联 Issue 和 Pull Request，直观跟踪任务分工与进度，适合敏捷小组协作。
👤 角色 1：后端核心 & 图数据架构师 (Backend Lead)
技术栈: Python, FastAPI, Neo4j (Driver), Pydantic
核心职责:
Neo4j Schema设计: 定义节点标签（Label）、关系类型（Relationship）及属性约束。
基础CRUD API: 实现知识点的增删改查接口（POST /nodes, GET /relations等）。
复杂查询封装: 编写高效的Cypher语句，封装为Python函数供其他模块调用（如：获取某节点的子图）。
数据验证: 使用Pydantic严格定义API输入输出模型。
交付物: app/api/graph.py, app/models/schema.py, Neo4j初始化脚本。
👤 角色 2：后端推理 & 数据工程 (Backend Engineer)
技术栈: Python, FastAPI, Neo4j Graph Algorithms, Pandas
核心职责:
推理引擎: 实现学习路径推荐（最短路径算法）、前置知识检测逻辑。
数据导入工具: 编写脚本将Excel/JSON课程数据批量清洗并导入Neo4j。
性能优化: 为常用查询字段建立Neo4j索引，优化慢查询。
辅助AI接口: 为AI模块提供“检索增强（RAG）”所需的上下文数据接口。
交付物: app/services/reasoning.py, scripts/import_data.py, 性能测试报告。
👤 角色 3：前端可视化专家 (Frontend Viz Lead)
技术栈: React/Vue, AntV G6 / ECharts Graph, TypeScript
核心职责:
图谱渲染核心: 负责力导向图、层级图、时间线三种视图的切换与渲染逻辑。
交互逻辑: 实现节点拖拽、缩放、点击展开详情、高亮关联路径等交互。
数据适配: 将后端返回的JSON数据转换为可视化库所需的Graph Data格式。
性能调优: 处理大规模节点渲染时的卡顿问题（如使用Web Worker或分层渲染）。
交付物: src/components/KnowledgeGraph.vue/ts, src/utils/graphAdapter.ts。
👤 角色 4：前端业务 & 交互界面 (Frontend UI Lead)
技术栈: React/Vue, Element Plus/AntD, Axios, State Management (Pinia/Redux)
核心职责:
系统框架: 搭建项目骨架，路由配置，全局状态管理。
业务页面: 开发用户管理、知识点编辑表单、搜索栏、自然语言输入框界面。
API联调: 统一封装Axios请求拦截器，处理Token认证和错误提示。
响应式布局: 确保系统在电脑/平板上的展示效果。
交付物: src/views/Editor.vue, src/store/, src/api/ (前端接口层)。
👤 角色 5：AI智能服务工程师 (AI Engineer)
技术栈: Python, LangChain, LLM API (通义/智谱), Embeddings
核心职责:
NL2Cypher Agent: 利用LangChain构建Agent，将用户自然语言转为Cypher查询语句（核心难点）。
内容生成: 调用LLM自动补全知识点描述、生成摘要。
Prompt工程: 设计并优化System Prompt，确保输出格式稳定（特别是Cypher语句的语法正确性）。
异常处理: 当LLM生成错误Cypher时，设计重试或降级机制。
交付物: app/services/llm_agent.py, prompts/ (提示词模板), AI模块单元测试。
👤 角色 6：DevOps & 项目管理 (DevOps & PM)
技术栈: Docker, Docker Compose, GitHub Actions, GitHub Projects
核心职责:
容器化编排: 编写docker-compose.yml，一键启动 Neo4j, FastAPI, Frontend, (可选VectorDB)。
CI/CD流水线: 配置GitHub Actions，实现代码提交后自动运行测试、自动构建镜像。
项目管理: 维护GitHub Projects看板，分配Issue，跟踪Bug，组织每日站会。
文档整合: 汇总API文档（Swagger）、部署手册、用户手册，制作演示视频。
数据内容填充: 协助整理课程知识数据（这是最耗时的人工活）。
交付物: Dockerfiles, docker-compose.yml, .github/workflows/, 项目Wiki/Readme。

github链接:https://github.com/cx8688688/Knowledge-Engineering-A.git

我是团队组长，我负责角色1这部分
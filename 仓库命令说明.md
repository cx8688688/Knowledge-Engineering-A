# Git / GitHub 仓库操作命令说明

> 仓库名：Knowledge-Engineering-A  
> 当前路径示例：H:\\document\\研1\\知识工程-A
>
> 说明：下面的命令按“初始化 → 日常开发 → 推送协作”顺序整理；已经执行过的步骤可以跳过，但文档保留完整流程，方便新环境/新成员使用。

---

## 一、仓库初始化与远程关联（已完成，可作为参考）

在一个全新的本地目录中，如果需要从 0 初始化并关联远程：

```powershell
# 1. 初始化本地 Git 仓库
cd H:\document\研1\知识工程-A
git init

# 2. 将当前分支命名为 main（统一分支名）
git branch -M main

# 3. 关联远程仓库（只需执行一次）
git remote add origin https://github.com/cx8688688/Knowledge-Engineering-A.git

# 4. 查看远程是否设置成功
git remote -v
```

> 当前仓库已经完成了上述步骤，一般不需要重复执行；只有在新机器上重新初始化时才需要。

---

## 二、项目架构搭建相关命令示例

> 注意：目录和文件可以通过 VS Code 资源管理器直接新建，这里给出命令行版本，方便需要时一次性创建。

### 2.1 顶层目录结构

```powershell
# 在仓库根目录下创建顶层目录
mkdir backend
mkdir frontend
mkdir docs
mkdir scripts
mkdir .github
mkdir .github\workflows
```

### 2.2 后端基础目录结构

```powershell
# backend 目录
cd backend
mkdir app
mkdir app\models
mkdir app\services
mkdir app\api
mkdir app\utils
mkdir tests

# 返回仓库根目录
cd ..
```

### 2.3 关键文件占位（示例）

> 占位文件可以先创建空文件，后续再在 VS Code 中补充具体代码。

```powershell
# 在仓库根目录下
# README 和 .gitignore
ni README.md -ItemType File
ni .gitignore -ItemType File

# 后端关键文件
ni backend\Dockerfile -ItemType File
ni backend\requirements.txt -ItemType File
ni backend\app\__init__.py -ItemType File
ni backend\app\main.py -ItemType File
ni backend\app\config.py -ItemType File
ni backend\app\models\__init__.py -ItemType File
ni backend\app\models\graph_schema.py -ItemType File
ni backend\app\models\neo4j_models.py -ItemType File
ni backend\app\services\__init__.py -ItemType File
ni backend\app\services\neo4j_driver.py -ItemType File
ni backend\app\services\graph_ops.py -ItemType File
ni backend\app\services\reasoning.py -ItemType File
ni backend\app\services\llm_agent.py -ItemType File
ni backend\app\api\__init__.py -ItemType File
ni backend\app\api\endpoints.py -ItemType File
ni backend\app\api\query_routes.py -ItemType File
ni backend\app\api\ai_routes.py -ItemType File
ni backend\app\utils\logger.py -ItemType File
ni backend\app\utils\exceptions.py -ItemType File
ni backend\tests\test_api.py -ItemType File
ni backend\tests\test_llm.py -ItemType File

# 文档与脚本占位
ni docs\api-spec.md -ItemType File
ni docs\schema-design.md -ItemType File
ni docs\user-manual.md -ItemType File
ni scripts\init-neo4j.cypher -ItemType File
ni scripts\import_data.py -ItemType File

# GitHub Actions 示例工作流占位
ni .github\workflows\ci-backend.yml -ItemType File
ni .github\workflows\deploy.yml -ItemType File
```

> 上述命令假设使用 PowerShell（Windows 默认）。如果已通过 VS Code 创建了一些目录/文件，对应命令可以跳过。

---

## 三、日常开发分支与提交流程

> 建议使用 main 作为稳定分支，dev 作为集成开发分支，个人或功能开发使用 feature/xxx 分支。

### 3.1 创建并推送开发分支 dev（建议尽早执行一次）

```powershell
# 基于 main 创建 dev 分支
git checkout -b dev

# 将 dev 分支推送到远程，并建立跟踪关系
git push -u origin dev
```

之后在 dev 分支上开发时，只需要：

```powershell
# 查看当前分支
git branch

# 拉取最新远程更新
git pull
```

### 3.2 开发流程通用命令

```powershell
# 1. 查看当前状态
git status

# 2. 查看本次改动详情
git diff         # 对比未暂存改动

# 3. 将所有新建/修改文件加入暂存区
git add .        # 或者精确指定文件：git add backend/app/main.py

# 4. 提交到当前分支（注意填写有意义的提交信息）
git commit -m "feat: init backend skeleton"

# 5. 推送到对应远程分支
git push         # 第一次可能需要：git push -u origin 当前分支名
```

> 提示：提交信息建议遵循固定前缀，例如 `feat:`, `fix:`, `chore:`, `docs:` 等，方便后续阅读历史。

---

## 四、查看提交历史与回滚相关命令

### 4.1 查看历史

```powershell
# 简要查看提交历史（单行模式）
git log --oneline

# 查看更详细的提交历史
git log
```

### 4.2 从旧提交中恢复某个文件（不改变当前历史）

```powershell
# 假设要从哈希为 abc1234 的提交中恢复某文件
# 1. 先查看历史，找到目标提交哈希
git log --oneline

# 2. 从旧提交中恢复文件到当前工作区（不会修改当前 HEAD）
git checkout abc1234 -- 路径/文件名
```

### 4.3 回退最近一次提交（仅在确认安全时使用）

```powershell
# 回退最近一次提交，但保留工作区文件改动
git reset --soft HEAD~1

# 回退最近一次提交，并丢弃工作区改动（危险）
git reset --hard HEAD~1

# 强制推送到远程（会改写远程历史，谨慎使用）
git push -f origin main
```

> 一般不建议频繁使用 `reset --hard` 和强制推送；团队协作时需要确认无人依赖当前远程历史。

---

## 五、从远程重新获取项目（在新机器或新环境）

```powershell
# 1. 在目标目录下克隆仓库
git clone https://github.com/cx8688688/Knowledge-Engineering-A.git

# 2. 进入项目目录
cd Knowledge-Engineering-A

# 3. 查看所有分支
git branch -a

# 4. 切换到 dev 分支进行开发（如有）
git checkout dev
```

---

## 六、与你当前角色最相关的建议

- 你作为后端/图数据架构负责人，推荐工作习惯：
  - 在 dev 分支上进行日常开发；
  - 为每一块较大的工作开 `feature/backend-schema`、`feature/graph-crud-api` 等分支；
  - 完成后在 GitHub 上发 Pull Request 合并到 dev，再由组长/自己检查后合并到 main。

- 与 Neo4j/后端相关的核心开发文件集中在：
  - backend/app/models/graph_schema.py
  - backend/app/models/neo4j_models.py
  - backend/app/services/neo4j_driver.py
  - backend/app/services/graph_ops.py
  - backend/app/api/endpoints.py
  - backend/app/api/query_routes.py

后续如果你需要，我可以再帮你生成一份“后端开发流程”专用文档，只讲 FastAPI + Neo4j 的启动、调试和常用命令。
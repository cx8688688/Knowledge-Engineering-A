# 知识工程-A 项目框架与推送步骤（角色 1）

> 目标：在本地构建好项目基础框架，并将当前目录初始化为 Git 仓库，推送到远程仓库 `Knowledge-Engineering-A`。

---

## 一、Git 仓库初始化并关联远程

1. 打开终端，进入当前目录：  
   `cd 知识工程-A`
2. 初始化本地 Git 仓库：  
   `git init`
3. 设置主分支名称为 main（如果默认不是）：  
   `git branch -M main`
4. 关联远程仓库（只需执行一次）：  
   `git remote add origin https://github.com/cx8688688/Knowledge-Engineering-A.git`
5. 检查远程是否设置成功：  
   `git remote -v`

> 说明：以上步骤假设远程仓库是空仓库或没有重要历史。如果远程已有代码，首次推送可能需要先拉取或处理冲突。

---

## 二、构建项目基础目录结构

在当前根目录（知识工程-A）下，创建项目框架目录：

1. 创建顶层目录：
   - `backend/`：后端 FastAPI + Neo4j 代码
   - `frontend/`：前端项目（Vue/React），先占位
   - `docs/`：文档（本体设计、API 说明等）
   - `deploy/`：部署相关文件（Docker、CI/CD 等）

2. 在 `backend/` 下创建后端骨架目录结构：
   - `backend/app/`
     - `main.py`：FastAPI 入口
     - `core/`：配置、Neo4j 连接等公共核心模块
       - `config.py`：读取环境变量（Neo4j 地址、账号密码等）
       - `neo4j.py`：Neo4j Driver 封装
     - `models/`：Pydantic 数据模型
       - `graph.py`：节点、关系、子图等模型
     - `api/`：路由层
       - `graph.py`：节点/关系 CRUD 与基础查询接口
     - `services/`：业务逻辑与图操作封装
       - `graph_service.py`：节点/关系增删改查与子图查询
   - `backend/scripts/`：后续数据导入、初始化脚本

3. 在 `docs/` 下创建本体/图谱 schema 文档（占位）：
   - `docs/knowledge-graph-schema-v0.1.md`
     - 描述节点类型（Course/Chapter/Section/Concept/...）
     - 描述关系类型（HAS_CHAPTER/PRE_REQUISITE/RELATED_TO/...）
     - 每类节点、关系的关键属性

---

## 三、最小可运行后端骨架（占位说明）

> 这一部分具体代码可以后续逐步补全，当前步骤先保证文件和结构存在。

1. 在 `backend/app/main.py` 中：
   - 初始化 FastAPI 应用实例
   - 添加一个简单的健康检查接口，例如：`GET /health` 返回 `{ "status": "ok" }`
   - 预留挂载 `api/graph.py` 路由的位置

2. 在 `backend/app/core/config.py` 中：
   - 预留从环境变量或配置文件读取 Neo4j 连接信息的接口

3. 在 `backend/app/core/neo4j.py` 中：
   - 预留创建 Neo4j Driver、获取 session 的函数签名

> 目前可以只写最小的占位代码，后续再逐步完善具体逻辑。

---

## 四、首次提交并推送到 GitHub

在完成上述目录与占位文件创建后，执行以下步骤完成首次推送：

1. 查看当前变更：  
   `git status`
2. 将所有文件加入暂存区：  
   `git add .`
3. 提交到本地 main 分支：  
   `git commit -m "chore: init project structure and steps doc"`
4. 将 main 分支推送到远程：  
   `git push -u origin main`

如果推送成功，GitHub 仓库页面上将能看到：
- 顶层目录：`backend/`、`frontend/`、`docs/`、`deploy/`、`任务.md`、`项目步骤.md`
- 首次提交记录：`chore: init project structure and steps doc`

---

## 五、后续你可以继续做的事（简要）

1. 在 `docs/knowledge-graph-schema-v0.1.md` 中详细设计知识图谱 schema。
2. 在 `backend/app` 内逐步补充 FastAPI + Neo4j 的真实逻辑。
3. 与其他角色对齐接口与目录，用本仓库作为统一协作基础。
